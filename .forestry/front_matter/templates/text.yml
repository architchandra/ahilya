---
label: Text
hide_body: true
fields:
- name: text
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: html
  label: Text
  description: Use the image block to add images (and not the image button below).
